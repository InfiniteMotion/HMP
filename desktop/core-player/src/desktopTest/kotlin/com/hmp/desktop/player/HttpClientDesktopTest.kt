// Moved to shared/src/commonTest - ktor dependencies only available in shared module
package com.hmp.desktop.player

import kotlin.test.Test
import kotlin.test.assertTrue

class HttpClientDesktopTest {
    @Test
    fun placeholder() {
        // HttpClient tests moved to shared module where ktor deps exist
        assertTrue(true)
    }
}
